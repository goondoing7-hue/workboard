# CLAUDE.md — 업무보드 작업 지침

## 이 프로젝트가 무엇인가

춘천시청소년상담복지센터 근무자(청소년상담사)가 **혼자 쓰는 업무 관리 도구**입니다.
판매용 서비스도, 여러 사람이 함께 쓰는 협업 도구도 아닙니다.

담는 것: 맡은 사업 → 세부사업 → 할 일, 그리고 사업마다 반드시 갖춰야 하는 행정서류 점검.
담지 않는 것: **내담자 정보, 상담 기록, 사례 내용** — 절대 이 앱에 들어가면 안 됩니다.

## 절대 하지 말 것

1. **내담자 개인정보를 다루는 기능을 추가하지 않는다.** 사례 관리, 상담 일지, 내담자 명단 같은 기능을 제안받더라도 만들지 않는다. 이 앱은 공개 URL에 암호 없이 올라가 있다.
2. **API 키를 코드에 넣지 않는다.** Supabase 키는 사용자가 앱 화면에서 직접 입력하고 그 기기의 localStorage에만 저장된다. 소스에 하드코딩하거나 환경변수로 빌드에 심지 않는다.
3. **localStorage 저장 구조(`workboard:data`)를 임의로 바꾸지 않는다.** 이미 실사용 중인 데이터가 있다. 구조를 바꿔야 하면 반드시 이전 형식을 읽어 변환하는 코드를 함께 넣는다.
4. **외부 CDN이나 추적 스크립트를 추가하지 않는다.** 모든 것이 단일 HTML에 인라인되어 오프라인에서 동작해야 한다.

## 반드시 지킬 것

### 행정서류 규칙 (업무 규정)
- 지출 **있음**: 사업계획서 → 지출품의서 → 영수증 → 세부지출내역서 → 지출결의서 → 결과보고서 (6종)
- 지출 **없음**: 사업계획서, 결과보고서 (2종)
- 지출 여부를 바꿔도 이미 체크한 사업계획서·결과보고서 상태는 유지한다.

### 데이터와 동기화
- 모든 변경에는 `updatedAt` 타임스탬프가 찍힌다. 기기 간 충돌은 **나중 시각이 이긴다**.
- 클라우드(Supabase)가 끊겨도 앱은 정상 동작해야 한다. 로컬이 항상 1순위, 클라우드는 그 뒤.
- 저장 실패나 동기화 실패는 **조용히 넘기지 말고** 화면에 알린다.

### 화면
- **모바일이 우선**이다. 실제로는 휴대폰에서 더 많이 쓴다. 터치 대상은 최소 44px.
- 색은 파일 상단 `C` 객체와 `PALETTE`에서만 가져온다. 새 색을 즉석에서 만들지 않는다.
- 파괴적 동작(삭제·완료)에는 **되돌리기**를 제공한다. `DeleteBtn`은 두 번 눌러야 지워진다.
- 날짜 표기: `8월 12일`, 시간은 `13:00` 또는 `13:00–15:00`, 남은 기간은 `D-3` / `오늘` / `2일 지남`.

### 말투
- 사용자에게 보이는 모든 문구는 **한국어**. 담백하고 사무적으로 쓰되 딱딱하지 않게.
- 느낌표와 이모지는 쓰지 않는다.

## 구조

```
src/app.jsx          앱 전체 (단일 파일, 약 1,600줄)
src/in.css           Tailwind 진입점
public/              아이콘, manifest, 서비스워커
scripts/build.js     JSX+CSS → dist/index.html 단일 파일로 묶음
vercel.json          배포 설정 + 보안 헤더
```

`app.jsx` 안의 순서: 색 토큰 → 유틸 → 공용 UI 조각 → 드래그 정렬 → 마감 편집 → 화면 컴포넌트 → `WorkBoard`(상태 전부 여기) → 하위 화면 → 설정.

상태는 `WorkBoard` 한 곳에 모여 있고 아래로 props를 내린다. 상태 관리 라이브러리를 도입하지 않는다.

## 작업 방식

- 고칠 때는 **파일 전체를 다시 쓰지 말고** 해당 부분만 수정한다.
- 컴포넌트를 다른 컴포넌트 **안에** 정의하지 않는다 (렌더마다 새로 만들어져 입력 포커스가 풀린다).
- 고친 뒤에는 `npm run build`가 통과하는지 확인한다.
- 기능을 더하기 전에 **정말 필요한지 먼저 묻는다.** 이 앱의 강점은 단순함이다.
